apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "typemill.fullname" . }}-test-connection"
  labels:
    {{- include "typemill.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '--timeout=10', 'http://{{ include "typemill.fullname" . }}:{{ .Values.service.port }}/']
  restartPolicy: Never
